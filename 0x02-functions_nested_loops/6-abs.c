#include "main.h"

/**
 * _abs - returns the absolute valute of input
 * @c: integer to be tested
 *
 * Description: accepts an input and gives out the absolute value of input
 * Return: Always 0
 */
int _abs(int c)
{
	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
	return (0)
}

