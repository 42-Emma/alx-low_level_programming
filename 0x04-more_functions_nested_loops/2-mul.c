#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first intger to be multiplied
 * @b: second integer to be multiplied
 * Return: the multiplied value
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
